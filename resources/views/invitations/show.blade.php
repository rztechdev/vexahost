@extends('layouts.auth', ['title' => 'Undangan Organisasi'])
@section('content')<div class="w-full max-w-md px-6"><div class="bg-white border border-slate-200 rounded-2xl p-8 text-center shadow-sm"><img src="{{ asset('images/logo.png') }}" alt="VexaHost" class="h-12 w-auto mx-auto mb-5"><h1 class="text-2xl font-bold">Undangan workspace</h1><p class="text-sm text-slate-500 mt-3">Anda diundang ke <strong>{{ $invitation->organization->name }}</strong> sebagai <strong>{{ $invitation->role->name }}</strong>.</p>@auth<form method="POST" action="{{ route('invitations.accept', $invitation->token) }}" class="mt-6">@csrf<button class="w-full rounded-lg bg-black px-4 py-2.5 text-sm font-bold text-white">Terima undangan</button></form>@else<a href="{{ route('login') }}" class="block mt-6 rounded-lg bg-black px-4 py-2.5 text-sm font-bold text-white">Login untuk menerima</a>@endauth</div></div>@endsection
